{
  "name": "featly-dashboard-smoke",
  "version": "0.0.0",
  "private": true,
  "description": "Headless smoke test for the embedded Featly dashboard: logs in, visits every routed screen, and fails on console/page errors or a screen that does not render.",
  "type": "module",
  "scripts": {
    "smoke": "node smoke.mjs"
  },
  "dependencies": {
    "playwright": "1.55.1"
  }
}
